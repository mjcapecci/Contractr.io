import React, { Fragment } from 'react';

import Layout from '../components/Layout';

const Landing = () => {
  return (
    <Fragment>
      <Layout></Layout>
    </Fragment>
  );
};

export default Landing;
